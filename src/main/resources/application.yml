spring:
  application:
    name: Picket

  datasource:
    url: jdbc:mysql://localhost:3306/picket
    username: root
    password: qhrtlfdl1!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        use_sql_comments: true
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true